syntax = "proto3";

package acme.example.tagdoc;

// Annotation from <environment> CoT detail element.
// Maps to ATAK's <zMist> inside <zMistsMap>.
message TagDocExample {
  // Field note from <sensor> (stays inside fence — unescaped).
  string name = 1;
}
