# Acme

Top-level namespace for the hand-maintained Acme example API corpus used by
`switchback-openrpc` multi-entry populate tests.

See versioned entry documents under `v1/`, `v2/`, and `v3alpha1/`.
