# Priority

Priority influences scheduler hints in narrative docs only.

```protobuf
enum [Priority](Priority.md) {
  PRIORITY_UNSPECIFIED = 0;
  PRIORITY_LOW = 1;
  PRIORITY_NORMAL = 2;
  PRIORITY_HIGH = 3;
  PRIORITY_CRITICAL = 4;
}
```

