# Acme Events v3alpha1

Alpha pipeline and experiment channels using **AsyncAPI 3.x** top-level
`operations` and **Avro** message payloads (`schemaFormat:
application/vnd.apache.avro+json`).

See `DEV-NOTES.md` for renderer and Avro fence expectations.
