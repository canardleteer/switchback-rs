#![forbid(unsafe_code)]

//! `switchback-asyncapi` CLI: parse an AsyncAPI document into a switchback.
//!
//!   switchback-asyncapi [--render mdbook] [--no-switchback] <input...>
//!
//! Always emits a switchback binary file unless `--no-switchback` is set. This binary
//! parses an AsyncAPI document into a switchback.

fn main() {
    eprintln!("switchback-asyncapi: unimplemented");
    std::process::exit(1);
}
