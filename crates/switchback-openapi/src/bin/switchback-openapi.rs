#![forbid(unsafe_code)]

//! `switchback-openapi` CLI: parse an OpenAPI Description into a switchback.
//!
//!   switchback-openapi [--render mdbook] [--no-switchback] <input...>
//!
//! Always emits a switchback binary file unless `--no-switchback` is set. This binary
//! parses an OpenAPI Description into a switchback.

fn main() {
    eprintln!("switchback-openapi: unimplemented");
    std::process::exit(1);
}
