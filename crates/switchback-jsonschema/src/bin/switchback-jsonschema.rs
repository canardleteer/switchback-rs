#![forbid(unsafe_code)]

//! `switchback-jsonschema` CLI: parse a JSON Schema catalog into a switchback.
//!
//!   switchback-jsonschema [--render mdbook] <input...>

fn main() {
    eprintln!("switchback-jsonschema: unimplemented");
    std::process::exit(1);
}
