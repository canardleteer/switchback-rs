#![forbid(unsafe_code)]

//! `switchback-openrpc` CLI: parse an OpenRPC document into a switchback.
//!
//!   switchback-openrpc [--render mdbook] [--no-switchback] <input...>
//!
//! Always emits a switchback binary file unless `--no-switchback` is set. This binary
//! parses an OpenRPC document into a switchback.

fn main() {
    eprintln!("switchback-openrpc: unimplemented");
    std::process::exit(1);
}
